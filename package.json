{
  "name": "millie",
  "private": true,
  "scripts": {
    "dev": "cd backend && npm run dev & cd frontend && npm run dev"
  },
  "dependencies": {
    "@langchain/core": "^1.1.40",
    "@langchain/groq": "^1.2.0"
  }
}
